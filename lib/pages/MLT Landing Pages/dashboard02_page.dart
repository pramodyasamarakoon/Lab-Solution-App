import 'package:flutter/material.dart';

class Dashboard02Page extends StatelessWidget {
  const Dashboard02Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dashboard 02'),
      ),
      body: const Center(
        child: Text('Dashboard 02 Content'),
      ),
    );
  }
}
